'use client'
import type { Session } from '@/types'

export function PerformanceSummary({ sessions: _sessions }: { sessions: Session[] }) {
  // ponytail: live sessions panel moved to dashboard page directly; this component reserved for future stats
  return null
}
